# VITAL Desktop v1.4.2 更新说明

本次更新以 `VITAL_v1.4.2-dev/server` 为行为基准，将桌面版从旧的随机加权抽词升级为 VITAL Ranker v0.2.1，并保持桌面端仅依赖 Python 标准库。

## 算法与学习流程

1. 新增 `vital_ranker.py`，移植服务端 `rule-v0.2.1` 的核心规则。
2. 桌面端 61 项 Ranker 默认参数与服务端逐项比对，全部一致。
3. 只有完全没有学习记录的词才是绝对新词；熟练度 1 的旧词归入复习。
4. 支持固定与自适应新词配额。
5. 加入遗忘风险、半衰期、历史增益、理想难度、不确定性、重要性、词频、词义数、tag 数、卡住词与冷却等特征。
6. 加入 weak、stuck、due_review、maintenance 分类及服务端相同的配额回填逻辑。
7. 加入 10% 探索、Softmax、MMR 多样性、词形家族去重、高难新词与卡住词上限。
8. 第二轮使用错位排列，避免词在两轮处于相同位置。
9. 文章目标长度改为 `180 + 35 × 新词数 + 18 × 复习词数`。
10. 增加 `selectionAudit.jsonl` 推荐审计。

## 词库

1. 新增 `vocabulary_tags.py`，统一 8 个服务端考试 tag、中文名称和难度。
2. 重写 `retrieve.py`，按大小写无关单词合并重复条目的 tag 后再执行 any/all。
3. 生成词库不再删除 `tag` 列。
4. 新增 `vocabulary_store.py`，支持命名词库、快照、切换、备份、当前状态和删除保护。
5. 旧 `vocabulary.csv` 首次启动自动登记为“旧版当前词库”。
6. 已有学习事件引用的词库禁止删除。

## 学习历史

1. 新增 `learning_history.py`。
2. `learningHistory.csv` 从两列汇总扩展为熟练度、学习次数、最近两次评分、首次/最近学习时间。
3. 新增 `learningEvents.csv` 保存逐会话、逐单词事件。
4. 旧两列历史可直接读取；缺少时间时使用原文件修改时间作为兼容估计。
5. 详细事件可在旧桌面版覆盖汇总文件后修复新版本状态。
6. 保留旧 `learning_flow.ensure_history_file/load_history/save_history/select_words` 公共接口兼容包装。

## 设置与界面

1. 默认值由 20/10 改为服务端 20/3。
2. 只有仍使用旧精确默认 20/10 的配置会迁移；自定义值（包括 0 新词）保留。
3. 新增 fixed/adaptive 选择及中文说明。
4. 旧固定 300 词默认提示词迁移到动态文章长度提示词。
5. 重做主界面，展示 VITAL 中英文全称、AI 辅助、自适应学习和开发者信息。
6. 重做词库管理页，加入中文考试范围、多词库列表、后台生成和状态显示。
7. 重做学习历史页，加入搜索与熟练度筛选。
8. 文章页显示目标长度并保留底部固定主按钮、失败重试和阅读统计。
9. 当前词库摘要中的 tag 使用中文显示。

## 启动与依赖

1. 删除桌面版 `start.bat` 和 `start.ps1`。
2. README 和界面统一说明使用 `python main.py`。
3. 未新增任何第三方 Python 库；仍只使用标准库和 Tkinter。

## 文档与测试

1. 全面重写 `README.md`，说明词库语义、Ranker 分类与排序、配额、探索、第二轮顺序、动态文章长度、数据文件、兼容迁移和测试方式。
2. 新增 `tests/test_core.py`。
3. `python -m compileall -q .` 通过。
4. 15 项标准库 `unittest` 全部通过。
5. 主窗口、设置、词库管理、词义学习、文章学习均通过虚拟显示环境烟雾测试。
6. 自动检查确认桌面项目无 `.bat`、`.ps1` 和第三方 Python 导入。
7. 完整工程包中的服务端原有 18 项 `pytest` 也全部通过，确认桌面同步没有影响服务端。
