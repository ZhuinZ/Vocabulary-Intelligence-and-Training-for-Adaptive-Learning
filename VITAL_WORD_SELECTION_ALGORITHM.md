# VITAL 单词赋权、词库抽取与每轮选词算法说明

> 适用实现：VITAL Desktop v1.4.2，推荐策略 `rule-v0.2.1`  
> 主要代码：`retrieve.py`、`vocabulary_tags.py`、`vital_ranker.py`、`learning_flow.py`  
> 本文描述的是当前代码的实际行为，而不是概念性建议。

## 1. 文档目的

VITAL 的选词不是简单地“从低分词中随机抽取”，而是一个分层流程：

1. 从 ECDICT 的 `stardict.csv` 按考试标签生成桌面端词库；
2. 读取当前词库，并按大小写无关方式去重；
3. 根据学习历史，把词划分为绝对新词、薄弱复习、卡住词、到期复习和巩固复习；
4. 对绝对新词和复习词分别计算特征与综合推荐分；
5. 计算本轮新词配额及各复习类别配额；
6. 在每个配额槽位中结合推荐分、词族/标签多样性和概率探索挑选单词；
7. 在库存不足或约束冲突时逐步放宽条件并回填；
8. 为第二轮学习生成与第一轮完全错位的顺序；
9. 将选词原因、特征、分数和概率写入 `selectionAudit.jsonl`。

推荐分统一限制在 `[0,1]`。分数越高，表示该词在其所属类别的候选池中越值得被选；但最终结果还受到类别配额、多样性、冷却、难词上限、卡住词上限和探索随机性的共同影响。因此，“综合分最高”并不等于“一定入选”。

---

## 2. 记号与基础函数

本文使用以下记号：

- `clip(x,a,b)=min(max(x,a),b)`：把数值截断到 `[a,b]`；未写上下界时默认 `[0,1]`。
- `N`：本轮总词数。
- `U_base`：设置中的基础绝对新词数。
- `U_eff`：固定或自适应计算后的有效绝对新词配额。
- `s₁`：某次学习的第一次词义评分，范围 1–5。
- `s₂`：同一次学习的第二次词义评分，范围 1–5。
- `n`：该词已学习次数。
- `Δt`：时间间隔，单位为天。
- `d`：历史衰减系数，默认 `0.80`。
- `I`：单词重要性。
- `S`：候选词最终推荐分。

代码中的“新词”严格指**绝对新词**：学习历史中不存在该词，或其 `study_count <= 0`。曾经学习过但熟练度为 1 的词仍然是复习词，不会占用新词配额。

---

## 3. 从 ECDICT 抽取桌面词库

### 3.1 允许使用的考试标签

| 标签代码 | 中文名称 | 默认难度 |
|---|---|---:|
| `zk` | 中考 | 0.15 |
| `gk` | 高考 | 0.25 |
| `cet4` | 大学英语四级 | 0.35 |
| `cet6` | 大学英语六级 | 0.50 |
| `ky` | 考研英语 | 0.50 |
| `ielts` | 雅思 | 0.65 |
| `toefl` | 托福 | 0.65 |
| `gre` | GRE | 0.85 |

标签难度只参与**绝对新词难度估计**，不意味着 GRE 词必然比所有其他词更优先。

### 3.2 标签标准化

输入标签可用空格、英文逗号、中文逗号、英文分号或中文分号分隔。处理时：

1. 去除首尾空白；
2. 转为大小写无关形式；
3. 按首次出现顺序去重；
4. 拒绝不在允许列表中的标签。

### 3.3 为什么需要两遍扫描

ECDICT 可能让同一个单词以多行、不同大小写或不同标签出现。VITAL 先按 `word.casefold()` 合并同一个词的标签，再判断是否匹配用户选择的考试范围。

设用户选择的标签集合为 `Q`，某个单词跨全部重复行合并后的相关标签集合为 `T(w)`：

- **任一范围（any）**：

  `w` 入选当且仅当 `T(w) ∩ Q ≠ ∅`。

- **全部范围（all）**：

  `w` 入选当且仅当 `Q ⊆ T(w)`。

第一遍只收集每个单词出现过的目标标签；第二遍才为已入选单词合并完整标签并保留字段。这样既与 Server 端“先合并、后筛选”的语义一致，又不必一次把完整 ECDICT 全部装入内存。

### 3.4 重复词条的合并规则

对于大小写无关后相同的单词：

- 展示拼写采用第一遍首次看到的拼写；
- `tag` 列合并该词在所有源行中的全部标签，并排序后以空格连接；
- 其他字段使用第二遍扫描中最后一行的非空/空字符串标准化结果；
- 输出按单词大小写无关字典序排列；
- 输出文件保留 ECDICT 原始表头，至少必须包含 `word` 和 `tag`。

### 3.5 进入推荐器前的再次清理

读取 `vocabulary.csv` 时，VITAL 会：

- 尝试 `UTF-8 with BOM` 和普通 `UTF-8`；
- 要求存在 `word` 列；
- 删除空单词；
- 按 `casefold()` 去重，只保留第一次出现的词条；
- 将所有字段转成字符串。

`build_selection_plan()` 内部还会再次去重，避免外部调用者传入重复数据。

---

## 4. 候选词分类总览

每个有效词条只会进入一个主类别：

| 内部类别 | 中文含义 | 判定摘要 |
|---|---|---|
| `unseen` | 绝对新词 | 无历史记录或 `study_count <= 0` |
| `stuck` | 卡住词 | 最近若干次提升很低且第二轮评分持续偏低 |
| `weak` | 薄弱复习 | 最近第二次评分 ≤2，或最近第一次评分为 1，或预测记忆 ≤0.20 |
| `due_review` | 到期复习 | 不属于前两类，且预测记忆 <0.60 |
| `maintenance` | 巩固复习 | 其余已学习词 |

判断顺序非常重要：先判断卡住词，再判断薄弱词，再判断到期词，最后才是巩固词。

---

## 5. 所有候选词共享的基础特征

### 5.1 词频重要性 `F`

VITAL 依次尝试从以下字段读取正数排名：

`frq → bnc → frequency → rank`

若候选词中有 `m` 个词具备排名，则按排名从小到大排序。第 `i` 个词（从 `0` 开始）的词频重要性为：

$$
F_i = 1-\frac{i}{m-1}
$$

因此，排名越靠前，`F` 越接近 1。只有一个有排名的词时，其 `F=1`。

若某词没有上述排名，则回退到 Collins 星级：

$$
F=\operatorname{clip}\left(\frac{\text{collins}}{5}\right)
$$

没有 Collins 值时使用 `2.5`，因此默认 `F=0.5`。

> 注意：有排名的词会在“有排名的词集合”内部线性归一化；没有排名的词使用 Collins 独立回退，两组并不是统一的全球百分位。

### 5.2 词义数量

把 `translation` 与 `definition` 拼接，用换行、`/`、英文分号或中文分号切分。有效片段数被限制在 1–6：

$$
C_{sense}=\operatorname{clip}_{[1,6]}(\text{有效释义片段数})
$$

### 5.3 标签广度优先级 `T_count`

设单词拥有 `k` 个去重标签，默认饱和值 `K=6`：

$$
T_{count}=\operatorname{clip}\left(\frac{\ln(1+k)}{\ln(1+K)}\right)
$$

无标签时为 0。对数形式意味着从 1 个增加到 2 个标签的收益，大于从 5 个增加到 6 个标签的收益。

### 5.4 单词重要性 `I`

基础重要性为：

$$
I_{base}=\operatorname{clip}(0.50\times1.0+0.35F+0.15\times0.50)
$$

即：

$$
I_{base}=\operatorname{clip}(0.575+0.35F)
$$

其中固定的 `1.0` 是核心词汇先验，固定的 `0.50` 是通用中性先验。再将标签广度混入：

$$
I=\operatorname{clip}((1-w_{tag})I_{base}+w_{tag}T_{count})
$$

默认 `w_tag=0.20`，所以：

$$
I=\operatorname{clip}(0.80I_{base}+0.20T_{count})
$$

### 5.5 词族键与候选相似度

VITAL 为多样性控制构造简化词族键：

1. 单词转小写，只保留 `a-z`；
2. 长度不超过 5 时直接使用；
3. 否则尝试删除一组常见后缀，如 `-ation`、`-ment`、`-ness`、`-ing`、`-ed`、`-s` 等，但删除后词干至少保留 5 个字母；
4. 最终最多取前 7 个字母。

两个候选词的相似度为：

$$
\operatorname{sim}(a,b)=
\begin{cases}
1.00,& \text{同一个单词}\\
0.90,& \text{词族键相同}\\
0.50\times\frac{|Tags_a\cap Tags_b|}{|Tags_a\cup Tags_b|},& \text{两者均有标签}\\
0,& \text{其他}
\end{cases}
$$

同词在前面已经去重，因此实际选词时最常见的是词族相似度和标签 Jaccard 相似度。

---

## 6. 绝对新词的赋权

### 6.1 学习者能力 `A`

取所有单词最近 50 条学习事件，按完成时间排序。每条事件只使用第一次评分衡量跨会话真实保持程度：

$$
q_j=\frac{s_{1,j}-1}{4}
$$

使用历史衰减 `d=0.80` 计算加权平均：

$$
\bar q=\frac{\sum_{j=1}^{m}d^{m-j}q_j}{\sum_{j=1}^{m}d^{m-j}}
$$

再计算第一次评分至少为 4 的比例：

$$
r_{mastery}=\frac{\#\{j:s_{1,j}\ge4\}}{m}
$$

最终能力：

$$
A=\operatorname{clip}(0.70\bar q+0.30r_{mastery})
$$

没有学习事件时使用 `A=0.35`。

### 6.2 新词客观难度 `D_new`

#### 罕见度

$$
R=1-F
$$

#### 长度难度

只统计英文字母，设字母数为 `L`：

$$
D_{length}=\operatorname{clip}\left(\frac{L-4}{8}\right)
$$

4 个及以下字母为 0，12 个及以上字母为 1。

#### 多义难度

$$
D_{sense}=\operatorname{clip}\left(\frac{C_{sense}-1}{5}\right)
$$

#### 标签难度

若单词的标签难度值为 `t₁,...,t_k`：

$$
D_{tag}=0.70\max(t_1,...,t_k)+0.30\frac{1}{k}\sum_{i=1}^{k}t_i
$$

无标签时使用 `0.50`。

#### 综合难度

$$
D_{new}=\operatorname{clip}(0.60R+0.20D_{length}+0.10D_{sense}+0.10D_{tag})
$$

词频/罕见度占 60%，是当前实现中影响最大的难度来源。

### 6.3 目标难度与难度匹配

目标难度比当前能力略高：

$$
D_{target}=\operatorname{clip}(A+\delta)
$$

默认挑战偏移 `δ=0.10`。匹配度使用高斯函数：

$$
M_{difficulty}=\exp\left(-\frac{(D_{new}-D_{target})^2}{2\sigma_{new}^2}\right)
$$

默认 `σ_new=0.20`。候选难度越接近目标难度，匹配度越接近 1；过易或过难都会下降。

### 6.4 知识脚手架 `C_scaffold`

只把当前熟练度至少为 4 的词视为“已掌握词”。

$$
C_{scaffold}=
\begin{cases}
0.85,& \text{与任一已掌握词的词族键相同}\\
0.70,& \text{与已掌握词共享至少一个标签}\\
0.50,& \text{其他}
\end{cases}
$$

这使新词更可能建立在已有词族或考试范围知识之上。

### 6.5 覆盖缺口 `C_coverage`

对新词的每个标签 `t`，统计当前词库中该标签总词数 `N_t` 与已掌握词数 `M_t`：

$$
C_t=1-\frac{M_t}{\max(1,N_t)}
$$

新词的覆盖缺口是其所有标签缺口的平均值：

$$
C_{coverage}=\frac{1}{|Tags|}\sum_{t\in Tags}C_t
$$

无标签时使用 `0.50`。某考试范围掌握得越少，其词的覆盖缺口越大。

### 6.6 绝对新词最终分数

$$
S_{new}=\operatorname{clip}\left(
\frac{
w_I I+w_M M_{difficulty}+w_S C_{scaffold}+w_C C_{coverage}
}{w_I+w_M+w_S+w_C}
\right)
$$

默认权重之和正好为 1：

$$
S_{new}=0.40I+0.35M_{difficulty}+0.15C_{scaffold}+0.10C_{coverage}
$$

因此，新词优先级依次更看重：重要性、难度是否适合、能否借助已有知识、是否补足覆盖缺口。

### 6.7 新词示例

假设某词：

- 词频重要性 `F=0.80`；
- 有 2 个标签：高考和四级；
- 8 个字母；
- 3 个释义片段；
- 学习者能力 `A=0.55`；
- 与已掌握词共享标签，因此脚手架 `0.70`；
- 覆盖缺口 `0.60`。

按当前公式得到：

- 标签广度优先级约 `0.5646`；
- 重要性约 `0.7969`；
- 新词难度约 `0.2935`；
- 目标难度 `0.65`；
- 难度匹配度约 `0.2042`；
- 最终新词分约 `0.5552`。

这个例子说明：一个词即使重要性较高，只要明显低于或高于当前目标难度，难度匹配项仍可能拉低总分。

---

## 7. 复习词的赋权

### 7.1 基础半衰期

最近第二次评分对应的基础记忆半衰期为：

| 最近第二次评分 | 基础半衰期（天） |
|---:|---:|
| 1 | 0.5 |
| 2 | 1.5 |
| 3 | 4.0 |
| 4 | 12.0 |
| 5 | 35.0 |

学习次数会温和提高先验半衰期：

$$
H_{prior}=H_0(s_2)\left(1+0.20\ln(1+n)\right)
$$

### 7.2 从跨会话保持率反推观察半衰期

对同一单词相邻两次学习事件，前一次第二次评分为 `s₂,prev`，后一次第一次评分为 `s₁,cur`：

$$
r=\operatorname{clip}_{[0.05,0.99]}\left(
\frac{s_{1,cur}-1}{\max(1,s_{2,prev}-1)}
\right)
$$

若前一次第二次评分 ≤1，或两次事件没有正时间间隔，则跳过该转移。

由指数遗忘模型反推观察半衰期：

$$
H_{obs}=\operatorname{clip}_{[H_{min},H_{max}]}
\left(\frac{\Delta t}{-\log_2 r}\right)
$$

默认限制为 0.5–365 天。越新的观察权重越大：

$$
w_j=d^{m-j}
$$

### 7.3 个体化半衰期融合

VITAL 在对数空间融合先验与观察，相当于加权几何平均：

$$
\ln H=\frac{
\alpha_H\ln H_{prior}+\sum_jw_j\ln H_{obs,j}
}{
\alpha_H+\sum_jw_j
}
$$

$$
H=\operatorname{clip}_{[H_{min},H_{max}]}(e^{\ln H})
$$

默认先验强度 `α_H=3.0`。历史很少时更信任评分先验；有效转移增加后，个人观察逐渐产生更大影响。

### 7.4 预测记忆与遗忘风险

最近第二次评分先映射到初始记忆：

$$
M_0=\frac{s_2-1}{4}
$$

经过 `t` 天后的预测记忆：

$$
M=\operatorname{clip}\left(M_0\cdot2^{-t/H}\right)
$$

遗忘风险：

$$
F_{forget}=1-M
$$

### 7.5 理想复习难度

VITAL 不只追求“最容易忘的词”，还偏好在一个适合努力回忆的记忆区间复习。默认目标记忆 `μ=0.45`，宽度 `σ=0.22`：

$$
D_{desirable}=\exp\left(-\frac{(M-\mu)^2}{2\sigma^2}\right)
$$

当预测记忆接近 0.45 时，该项接近 1；记忆仍很牢或已经几乎完全遗忘时，该项降低。

### 7.6 单次学习增益

对于同一次学习的两次评分：

$$
g_{raw}=\frac{\max(0,s_2-s_1)}{4}
$$

$$
g_{headroom}=\frac{\max(0,s_2-s_1)}{\max(1,5-s_1)}
$$

综合单次增益：

$$
g=\operatorname{clip}\left(
\frac{w_rg_{raw}+w_hg_{headroom}}{w_r+w_h}
\right)
$$

默认两个权重均为 0.50。`g_raw` 衡量绝对提升幅度，`g_headroom` 衡量利用了多少可提升空间。

### 7.7 期望学习增益 `G`

每个词最多取最近 `gain_window=8` 次事件，越新的增益权重越大。加入均值 `0.35`、强度 `3.0` 的先验：

$$
G=\frac{
\alpha_G\mu_G+\sum_jw_jg_j
}{
\alpha_G+\sum_jw_j
}
$$

默认 `α_G=3.0`、`μ_G=0.35`。这避免只有一两次异常高提升就让该词获得极端高分。

### 7.8 评分下降惩罚

若第二次评分反而低于第一次评分：

$$
p_j=\frac{\max(0,s_{1,j}-s_{2,j})}{4}
$$

最近事件的下降惩罚使用衰减加权平均：

$$
P_{drop}=\frac{\sum_jw_jp_j}{\sum_jw_j}
$$

### 7.9 不确定性

$$
U=\frac{1}{\sqrt{1+n}}
$$

学习次数越少，不确定性越高。该项让历史不足的词获得一定探索机会，而不是永久被确定性更高的旧词压制。

### 7.10 卡住词判定

取最近 `W_stuck=3` 次事件：

$$
\bar g_{stuck}=\frac{1}{W_{stuck}}\sum g_j
$$

$$
\bar s_{2,stuck}=\frac{1}{W_{stuck}}\sum s_{2,j}
$$

同时满足下列条件时为卡住词：

$$
\bar g_{stuck}<0.10
\quad\text{且}\quad
\bar s_{2,stuck}\le2.0
$$

卡住词仍可进入薄弱槽位，但会被扣分并受单轮数量上限限制，避免用户连续面对大量几乎没有学习收益的词。

### 7.11 复习类别判定

按以下顺序：

1. 若满足卡住词条件：`stuck`；
2. 否则若 `s₂≤2`、最近第一次评分 `s₁=1`，或 `M≤0.20`：`weak`；
3. 否则若 `M<0.60`：`due_review`；
4. 否则：`maintenance`。

### 7.12 复习词最终分数

基础复习分：

$$
B=\frac{
w_FF_{forget}+w_GG+w_DD_{desirable}+w_UU
}{w_F+w_G+w_D+w_U}
$$

默认权重和为 1：

$$
B=0.45F_{forget}+0.25G+0.20D_{desirable}+0.10U
$$

再按单词重要性缩放：

$$
B_I=B(0.65+0.35I)
$$

最终分数：

$$
S_{review}=\operatorname{clip}\left(
B_I-0.10P_{drop}-0.20\cdot\mathbf{1}_{stuck}
\right)
$$

卡住词扣分不是忽略它，而是与卡住词槽位上限一起防止高频重复挫败。

### 7.13 冷却规则

如果：

- 距离最近一次学习少于 12 小时；并且
- 最近第二次评分高于 2；

则候选标记为 `cooldown=True`。正常抽取阶段优先排除它；只有候选不足、约束逐步放宽时才允许进入。

### 7.14 复习词示例

假设某词：

- 最近第一次评分 3、第二次评分 4；
- 共学习 3 次；
- 距最近学习 6 天；
- 尚无可用的跨会话半衰期观察；
- 最近两次事件分别为 `2→4` 和 `3→4`；
- 重要性沿用前例 `0.7969`；
- 没有评分下降，也不是卡住词。

计算结果：

- 半衰期先验约 `15.3271` 天；
- 当前预测记忆约 `0.5718`；
- 遗忘风险约 `0.4282`；
- 理想复习难度约 `0.8580`；
- 期望学习增益约 `0.3941`；
- 不确定性 `0.5000`；
- 最终复习分约 `0.4764`。

其预测记忆介于 0.20 和 0.60 之间时，会被划入到期复习；若已经低于 0.20，则转为薄弱复习。

---

## 8. 固定与自适应新词配额

### 8.1 固定模式

`new_word_mode="fixed"` 时：

$$
U_{eff}=\operatorname{clip}_{[0,N]}(U_{base})
$$

### 8.2 自适应模式启用条件

仅当：

- `new_word_mode="adaptive"`；并且
- 至少有 3 个历史会话日期；

才启用自适应计算。否则退回基础新词数。

### 8.3 学习间隔倍率

最多使用最近 20 个会话日期，忽略不超过 1 分钟的间隔。设有效历史间隔中位数为 `G_typical`，距最近会话的当前间隔为 `G_current`：

$$
r_{gap}=\max\left(0.01,\frac{G_{current}}{G_{typical}}\right)
$$

$$
m_{gap}=\operatorname{clip}_{[0.70,1.20]}\left(r_{gap}^{-0.25}\right)
$$

含义：

- 当前间隔比平时长，`m_gap<1`，减少新词；
- 当前间隔比平时短，`m_gap>1`，可略增新词；
- 影响被限制在 0.70–1.20。

### 8.4 近期跨会话保持率

对每个词的相邻事件：

$$
r_j=\operatorname{clip}\left(
\frac{s_{1,cur}-1}{\max(1,s_{2,prev}-1)}
\right)
$$

取所有词最近 50 个转移，按 `history_decay` 加权平均，得到 `R_recent`。没有转移时使用基线 `0.60`。

表现倍率：

$$
m_{performance}=\operatorname{clip}_{[0.75,1.20]}
\left(1+0.60(R_{recent}-0.60)\right)
$$

保持率高于 0.60 时增加新词，低于 0.60 时减少新词。

### 8.5 有效新词数

自适应模式最多允许新词占整轮的 25%：

$$
U_{max}=\min(N,\lceil0.25N\rceil)
$$

$$
U_{eff}=\operatorname{clip}_{[0,U_{max}]}
\left(\operatorname{round}(U_{base}m_{gap}m_{performance})\right)
$$

代码使用 Python 的 `round()`，恰好为 `.5` 时遵循“舍入到最近的偶数”。

---

## 9. 类别配额与槽位顺序

设复习词总槽位：

$$
R=N-U_{eff}
$$

默认配额：

$$
Q_{weak}=\min\left(R,\operatorname{round}(0.25N)\right)
$$

$$
Q_{maintenance}=\min\left(
R-Q_{weak},\operatorname{round}(0.10N)
\right)
$$

$$
Q_{due}=\max(0,R-Q_{weak}-Q_{maintenance})
$$

$$
Q_{unseen}=U_{eff}
$$

例如 `N=20`、`U_eff=3`：

- 绝对新词：3；
- 薄弱槽位：5；
- 巩固槽位：2；
- 到期槽位：10。

槽位不是按类别整块排列，而是按固定类别顺序循环穿插：

`unseen → weak → due_review → maintenance → unseen → weak → ...`

某类别配额用完后跳过。这样可避免第一轮中连续出现大量同类型词。

`stuck` 没有独立槽位，而是可以进入 `weak` 槽位。

---

## 10. 单轮限制

### 10.1 卡住词上限

$$
L_{stuck}=\max\left(1,\lfloor N\times0.10\rfloor\right)
$$

例如 `N=20` 时最多 2 个，`N<10` 时仍最多允许至少 1 个。

### 10.2 高难绝对新词上限

绝对新词难度 `D_new≥0.75` 时视为高难新词。

根据当前间隔与典型间隔之比 `r_gap`：

| 间隔状态 | 条件 | 高难新词比例上限 |
|---|---|---:|
| 正常 | `r_gap≤1`，或无自适应间隔数据 | 0.40 |
| 略长 | `1<r_gap≤2` | 0.25 |
| 明显过长 | `r_gap>2` | 0.15 |

$$
L_{hard}=\left\lceil\max(1,U_{eff})\times r_{hard}\right\rceil
$$

候选不足时该限制可被放宽，因此它是优先约束而不是绝对禁止。

---

## 11. MMR 多样性与概率探索

### 11.1 MMR 调整分

对于当前候选 `i`，设其原始推荐分为 `S_i`，与已经选中词的最大相似度为：

$$
Sim_i=\max_{j\in Selected}\operatorname{sim}(i,j)
$$

MMR 调整分：

$$
A_i=\lambda S_i-(1-\lambda)Sim_i
$$

默认 `λ=0.80`：80% 看推荐分，20% 惩罚与已选词过于相似。

### 11.2 探索槽位数量

默认探索率 `ε=0.10`：

$$
E=\min\left(N,\max\left(1,\operatorname{round}(N\varepsilon)\right)\right)
$$

当 `ε>0` 时至少有 1 个探索槽位。系统先在全部槽位索引中均匀随机选出 `E` 个作为探索槽位。

### 11.3 利用（exploit）

普通槽位直接选择 MMR 调整分最大的候选：

$$
i^*=\arg\max_i A_i
$$

审计中记录的选择概率为：

$$
P(i^*)=(1-\varepsilon)+\varepsilon p_{i^*}
$$

### 11.4 探索（explore）

探索槽位先计算 Softmax：

$$
p_i=\frac{
\exp((A_i-A_{max})/T)
}{
\sum_k\exp((A_k-A_{max})/T)
}
$$

减去 `A_max` 只为数值稳定，不改变概率。默认温度 `T=0.15`。

随后按 `p_i` 加权随机抽取。审计中记录：

$$
P(i)=\varepsilon p_i
$$

> `selection_probability` 是推荐策略审计概率表达，不是对整套“先随机决定探索槽位、再经过约束与回填”流程做出的完整全局概率推导。

---

## 12. 每个槽位的候选过滤与逐步放宽

对一个目标类别槽位，先排除：

- 已经入选的单词；
- 正在冷却的词；
- 类别不匹配的词；
- 超过卡住词上限的卡住词；
- 超过高难新词上限的高难新词；
- 与已选词拥有相同词族键的词。

系统按以下四级依次尝试，找到非空候选池即停止放宽：

1. 不允许冷却词，不允许重复词族，不放宽高难新词限制；
2. 允许重复词族；
3. 再放宽高难新词限制；
4. 最后允许冷却词。

这种顺序表示：多样性约束可以先让步，高难新词约束其次，近期冷却最后让步。

---

## 13. 类别库存不足时的回填

目标类别完全没有候选时，按下表寻找替代类别：

| 原槽位 | 回填优先级 |
|---|---|
| 绝对新词 | 薄弱 → 到期 → 巩固 |
| 薄弱 | 到期 → 巩固 → 绝对新词 |
| 到期 | 薄弱 → 巩固 → 绝对新词 |
| 巩固 | 到期 → 薄弱 → 绝对新词 |

回填搜索会允许冷却、重复词族和高难新词。若仍无候选，则从所有尚未入选词中选择原始推荐分最高者。

若完成所有槽位后总数仍不足，会把剩余候选按原始推荐分降序排列，直接补满至 `N` 或词库总量。

因此，`requested_quotas` 是目标配额，`actual_category_counts` 才是实际结果。库存不足时二者可以不同。

---

## 14. 第一轮与第二轮顺序

第一轮顺序就是槽位逐个完成选择的顺序。

第二轮对第一轮索引做**错排（derangement）**：

$$
\forall i,\quad \pi(i)\ne i
$$

系统最多随机打乱 50 次寻找完全错位排列；若失败，则使用 1 到 `N-1` 的随机循环偏移。词数大于 1 时，每个词在第二轮的位置都不会与第一轮相同。

这只改变展示顺序，不改变入选词集合和推荐分。

---

## 15. 两次评分如何形成最终熟练度

一轮完成后，每个词的最终熟练度为：

$$
L=\operatorname{clip}_{[1,5]}\left(
\left\lfloor\frac{s_1+2s_2}{3}\right\rfloor
\right)
$$

第二次评分权重是第一次的两倍，因为它发生在文章阅读和再次接触之后，更能代表本轮结束时的掌握状态。

最终熟练度写入 `learningHistory.csv`；两次原始评分、会话时间和顺序写入 `learningEvents.csv`，供下一轮个体化半衰期、保持率、增益和能力估计使用。

---

## 16. 推荐审计文件

每轮开始前，系统向 `selectionAudit.jsonl` 追加一行 JSON，主要包括：

- `policy_version`：策略版本；
- 请求总词数、基础新词数和有效新词数；
- 请求配额与实际类别数量；
- 学习者能力和自适应倍率；
- 探索目标数、实际探索数和回填数；
- 卡住词与高难新词上限；
- 每个入选词的类别、原始分数、选择方式和概率；
- 预测记忆、半衰期、遗忘风险、增益、重要性、难度、覆盖缺口等特征；
- `display_order` 与 `second_display_order`；
- 可读的原因代码。

常见原因代码：

| 原因代码 | 含义 |
|---|---|
| `absolutely_unseen` | 绝对未学习 |
| `due_for_review` | 遗忘风险至少 0.50 |
| `desirable_difficulty` | 位于较理想的复习记忆区间 |
| `historically_learnable` | 历史期望增益至少 0.50 |
| `weak_memory` | 被划为薄弱复习 |
| `stuck_word` | 被判断为卡住词 |
| `important_word` | 重要性至少 0.75 |
| `broad_exam_coverage` | 至少有 2 个标签 |
| `uncertain_estimate` | 不确定性至少 0.50 |
| `difficulty_matched` | 新词难度匹配度至少 0.70 |
| `knowledge_scaffold` | 新词可利用词族或标签脚手架 |
| `coverage_gap` | 新词覆盖缺口至少 0.60 |
| `cooldown` | 处于近期学习冷却期 |
| `quota_fallback` | 目标类别库存不足而回填 |
| `final_fallback` | 所有槽位完成后仍不足而最终补齐 |

---

## 17. 完整流程伪代码

```text
输入：当前词库、汇总历史、逐次事件、会话日期、N、U_base、新词模式

1. 清理词库：删除空词；按 casefold 去重
2. N = min(max(1, N), 词库大小)
3. U_base = clip(U_base, 0, N)
4. 计算所有词的词频重要性
5. 用最近 50 条事件计算学习者能力 A
6. 固定或自适应计算 U_eff
7. 把熟练度 >= 4 的词作为已掌握词，统计已掌握词族、标签和各标签覆盖率

8. 对词库中的每个词：
   若无历史：
       计算重要性、难度、难度匹配、脚手架、覆盖缺口
       计算 S_new
       分类为 unseen
   否则：
       估计个体化半衰期与预测记忆
       计算遗忘风险、理想复习难度、期望增益、下降惩罚、不确定性
       判断 stuck / weak / due_review / maintenance
       计算 S_review

9. 计算 unseen、weak、due_review、maintenance 的目标配额
10. 按 unseen→weak→due→maintenance 循环生成槽位
11. 随机指定约 10% 的槽位为探索槽位
12. 计算卡住词上限与高难新词上限

13. 对每个槽位：
    a. 按类别、冷却、词族、难词上限、卡住上限过滤候选
    b. 若为空，依次放宽词族、难词和冷却约束
    c. 若仍为空，按类别优先级回填
    d. 普通槽位选择 MMR 调整分最高者
       探索槽位按 MMR 调整分的 Softmax 概率抽取
    e. 记录入选词、词族和计数

14. 若数量仍不足，按原始分数从剩余词中补齐
15. 为第二轮生成完全错位顺序
16. 写入 selectionAudit.jsonl
17. 返回第一轮和第二轮词表
```

---

## 18. 默认参数完整表

### 18.1 全局、随机性与多样性

| 参数 | 默认值 | 作用 |
|---|---:|---|
| `policy_version` | `rule-v0.2.1` | 推荐策略版本，写入审计文件，用于辨别不同算法版本。 |
| `history_decay` | `0.8` | 历史事件的时间衰减系数；越接近 1，较早记录保留的影响越大。 |
| `gain_window` | `8` | 计算近期学习增益与评分下降时，每个词最多取最近多少次事件。 |
| `cooldown_hours` | `12.0` | 最近第二次评分高于 2 的词，在此小时数内优先不再次抽取。 |
| `exploration_rate` | `0.1` | 一轮中用于概率探索的目标比例 ε。 |
| `softmax_temperature` | `0.15` | Softmax 温度 T；越小越偏向高分候选，越大越随机。 |
| `mmr_lambda` | `0.8` | MMR 中相关性权重 λ；越大越重视单词分数，越小越重视多样性。 |

### 18.2 类别配额与单轮上限

| 参数 | 默认值 | 作用 |
|---|---:|---|
| `weak_ratio` | `0.25` | 总词数中预留给薄弱复习槽位的比例。卡住词也从该槽位池中抽取。 |
| `maintenance_ratio` | `0.1` | 总词数中预留给巩固复习槽位的比例。 |
| `stuck_max_ratio` | `0.1` | 卡住词在一轮中的最大比例。实际至少允许 1 个。 |
| `hard_new_max_ratio_normal` | `0.4` | 学习间隔正常时，高难绝对新词占新词配额的最大比例。 |
| `hard_new_max_ratio_moderate` | `0.25` | 本次间隔略长时，高难绝对新词占新词配额的最大比例。 |
| `hard_new_max_ratio_late` | `0.15` | 本次间隔明显过长时，高难绝对新词占新词配额的最大比例。 |

### 18.3 记忆半衰期模型

| 参数 | 默认值 | 作用 |
|---|---:|---|
| `half_life_score_1_days` | `0.5` | 最近第二次评分为 1 时的基础记忆半衰期（天）。 |
| `half_life_score_2_days` | `1.5` | 最近第二次评分为 2 时的基础记忆半衰期（天）。 |
| `half_life_score_3_days` | `4.0` | 最近第二次评分为 3 时的基础记忆半衰期（天）。 |
| `half_life_score_4_days` | `12.0` | 最近第二次评分为 4 时的基础记忆半衰期（天）。 |
| `half_life_score_5_days` | `35.0` | 最近第二次评分为 5 时的基础记忆半衰期（天）。 |
| `half_life_prior_strength` | `3.0` | 基础半衰期先验在对数加权融合中的强度。 |
| `half_life_min_days` | `0.5` | 个体化半衰期的最小截断值（天）。 |
| `half_life_max_days` | `365.0` | 个体化半衰期的最大截断值（天）。 |

### 18.4 学习增益模型

| 参数 | 默认值 | 作用 |
|---|---:|---|
| `gain_prior_mean` | `0.35` | 历史不足时，期望学习增益的先验均值。 |
| `gain_prior_strength` | `3.0` | 学习增益先验强度。 |
| `gain_raw_weight` | `0.5` | 学习增益中“满量程提升”分量的权重。 |
| `gain_headroom_weight` | `0.5` | 学习增益中“利用剩余提升空间”分量的权重。 |

### 18.5 复习词评分与分类

| 参数 | 默认值 | 作用 |
|---|---:|---|
| `review_weight_forgetting` | `0.45` | 复习词综合分中遗忘风险的权重。 |
| `review_weight_gain` | `0.25` | 复习词综合分中期望学习增益的权重。 |
| `review_weight_difficulty` | `0.2` | 复习词综合分中理想复习难度的权重。 |
| `review_weight_uncertainty` | `0.1` | 复习词综合分中估计不确定性的权重。 |
| `target_memory` | `0.45` | 理想复习点的目标预测记忆值。 |
| `target_memory_width` | `0.22` | 理想复习点高斯曲线的宽度。 |
| `drop_penalty_weight` | `0.1` | 第二次评分低于第一次评分时，对复习分数施加的惩罚权重。 |
| `stuck_penalty_weight` | `0.2` | 卡住词分数惩罚；用于降低反复失败词的连续出现概率。 |
| `weak_memory_threshold` | `0.2` | 预测记忆不高于该值时，划入薄弱复习。 |
| `due_memory_threshold` | `0.6` | 预测记忆低于该值且不属于薄弱/卡住时，划入到期复习。 |
| `stuck_window` | `3` | 判断卡住词时查看的最近事件数。 |
| `stuck_gain_threshold` | `0.1` | 卡住窗口内平均学习增益必须低于此值。 |
| `stuck_second_score_threshold` | `2.0` | 卡住窗口内平均第二次评分必须不高于此值。 |

### 18.6 绝对新词评分

| 参数 | 默认值 | 作用 |
|---|---:|---|
| `new_weight_importance` | `0.4` | 绝对新词综合分中重要性的权重。 |
| `new_weight_difficulty_match` | `0.35` | 绝对新词综合分中难度匹配度的权重。 |
| `new_weight_scaffold` | `0.15` | 绝对新词综合分中知识脚手架的权重。 |
| `new_weight_coverage` | `0.1` | 绝对新词综合分中词库覆盖缺口的权重。 |
| `new_challenge_offset` | `0.1` | 新词目标难度相对当前能力的上浮量。 |
| `new_difficulty_width` | `0.2` | 新词难度匹配高斯曲线的宽度。 |
| `tag_count_weight` | `0.2` | 单词重要性中考试标签广度的权重。 |
| `tag_count_saturation` | `6` | 标签广度函数达到 1 时使用的饱和值。 |
| `tag_difficulty` | `{"cet4": 0.35, "cet6": 0.5, "gk": 0.25, "gre": 0.85, "ielts": 0.65, "ky": 0.5, "toefl": 0.65, "zk": 0.15}` | 各考试标签的默认难度映射。 |

### 18.7 自适应新词配额

| 参数 | 默认值 | 作用 |
|---|---:|---|
| `adaptive_session_window` | `20` | 自适应新词数计算典型学习间隔时最多使用的最近会话数。 |
| `adaptive_min_sessions` | `3` | 启用自适应新词数所需的最少会话日期数。 |
| `adaptive_recent_transition_window` | `50` | 估计近期跨会话保持率时最多使用的最近状态转移数。 |
| `adaptive_gap_exponent` | `0.25` | 学习间隔倍率对新词数的幂指数。 |
| `adaptive_gap_min_multiplier` | `0.7` | 间隔倍率的最小值。 |
| `adaptive_gap_max_multiplier` | `1.2` | 间隔倍率的最大值。 |
| `adaptive_retention_baseline` | `0.6` | 保持率基线；达到该值时表现倍率为 1。 |
| `adaptive_performance_slope` | `0.6` | 保持率相对基线变化对新词数倍率的斜率。 |
| `adaptive_performance_min_multiplier` | `0.75` | 表现倍率的最小值。 |
| `adaptive_performance_max_multiplier` | `1.2` | 表现倍率的最大值。 |
| `adaptive_new_max_ratio` | `0.25` | 自适应模式下绝对新词数不得超过整轮词数的比例。 |

### 18.8 文章长度（与抽词结果联动，但不参与赋权）

| 参数 | 默认值 | 作用 |
|---|---:|---|
| `article_base_words` | `180` | AI 阅读文章的基础目标词数；不参与抽词。 |
| `article_words_per_unseen` | `35` | 每个绝对新词增加的文章目标词数；不参与抽词。 |
| `article_words_per_review` | `18` | 每个复习词增加的文章目标词数；不参与抽词。 |


---

## 19. 参数调节方向

以下是调参方向，不代表应随意更改默认值：

- 增大 `history_decay`：更重视较早历史，个体模型变化更平缓；减小则更敏感于最近表现。
- 增大 `cooldown_hours`：减少短时间重复；过大可能导致小词库频繁回填冷却词。
- 增大 `exploration_rate` 或 `softmax_temperature`：增加随机探索；过大可能降低稳定性。
- 增大 `mmr_lambda`：更接近纯分数排序；减小则更积极避免相似词。
- 增大 `weak_ratio`：增加薄弱/卡住词复习；会压缩到期复习槽位。
- 增大 `maintenance_ratio`：增加高记忆词维护；会压缩到期复习槽位。
- 增大半衰期：同样学习间隔下预测记忆更高，词更晚进入到期/薄弱类别。
- 增大 `target_memory`：把理想复习点前移到记忆更牢时；减小则允许更晚复习。
- 增大 `target_memory_width`：更多记忆状态都会获得较高理想难度分。
- 增大 `review_weight_forgetting`：更偏向即将或已经遗忘的词。
- 增大 `review_weight_gain`：更偏向历史上每次复习后提升明显的词。
- 增大 `stuck_penalty_weight` 或减小 `stuck_max_ratio`：降低卡住词密度。
- 增大 `new_challenge_offset`：新词目标更具挑战；必须同时关注高难新词上限。
- 减小 `new_difficulty_width`：难度匹配更严格，远离目标难度的词分数下降更快。
- 增大 `new_weight_coverage`：更积极补齐尚未掌握的考试范围。
- 增大 `tag_count_weight`：多考试范围共有词的重要性更高。
- 增大 `adaptive_gap_exponent`：学习中断对新词数的削减更强。
- 增大 `adaptive_performance_slope`：近期保持率对新词数的影响更强。

调参时应同时观察 `selectionAudit.jsonl` 中的：实际类别数量、回填数量、探索数量、预测记忆分布、难度分布和重复词族情况。只看最终入选词，难以区分是权重变化还是配额/约束/回填造成的结果。

---

## 20. 必须保持的兼容与设计不变量

为保持与当前 Server 和旧桌面数据兼容，修改算法时应尽量维持以下不变量：

1. 单词主键按 `casefold()` 比较；
2. 绝对新词只由“是否存在有效学习记录”判断，不能把熟练度 1 当作新词；
3. ECDICT 重复词必须先合并标签，再进行 any/all 判断；
4. `tag` 列必须保留，否则标签难度、覆盖缺口和多样性都会失效；
5. 推荐分、概率和中间特征应保持在合理有限范围，避免 NaN/无穷值；
6. 库存不足时必须能回填，不能因为严格配额导致整轮无法开始；
7. 推荐审计失败不得阻塞核心学习流程；
8. 第二轮词集合必须与第一轮一致，只改变顺序；
9. 旧版两列 `learningHistory.csv` 中出现过的词仍然必须视为已学习；
10. 算法版本发生语义变化时，应更新 `policy_version`。

---

## 21. 与文章阅读长度的联动

文章长度不参与单词赋权，但由最终实际入选类别决定。设实际绝对新词数为 `U_actual`，复习词数为 `R_actual`：

$$
W_{article}=180+35U_{actual}+18R_{actual}
$$

因此，配额回填导致实际新词数变化时，文章目标长度也会随实际结果变化，而不是只依据设置中的基础新词数。

---

## 22. 实现位置索引

| 逻辑 | 文件/函数 |
|---|---|
| 标签白名单与标签难度 | `vocabulary_tags.py` |
| ECDICT 标签解析、两遍扫描、any/all、重复合并 | `retrieve.py`：`parse_tags()`、`export_vocabulary_by_tags()` |
| 词库读取与去重 | `learning_flow.py`：`load_vocabulary()` |
| 全部推荐参数 | `vital_ranker.py`：`RankerSettings` |
| 新词特征与评分 | `vital_ranker.py`：`_ability()`、`_word_difficulty()`、`_new_candidate()` |
| 复习记忆、增益、分类与评分 | `vital_ranker.py`：`_observed_half_life()`、`_review_candidate()` |
| 自适应新词数 | `vital_ranker.py`：`_adaptive_new_count()` |
| 配额、槽位、MMR、探索与回填 | `vital_ranker.py`：`_category_quotas()` 至 `build_selection_plan()` |
| 第二轮错排 | `vital_ranker.py`：`_assign_second_order()` |
| 最终熟练度与审计写入 | `learning_flow.py`：`_merge_scores()`、`_append_selection_audit()` |
| 文章目标长度 | `vital_ranker.py`：`article_target_word_count()` |

---

## 23. 一句话总结

VITAL 先用考试标签建立可解释的词库，再分别用“适合学习的新词价值模型”和“遗忘—增益—理想难度复习模型”为候选词赋分，最后通过类别配额、个体化新词数、MMR 多样性、少量概率探索和多级回填，生成既能复习薄弱点、又不会过度重复或一次塞入太多难词的一轮学习词表。
